# Setup

# Install CUDA toolkit (adjust version to match your driver)
sudo apt install nvidia-cuda-toolkit
# or for a specific version from NVIDIA's repo:
# https://developer.nvidia.com/cuda-downloads
